---
import ContentWidth from "../components/ContentWidth.astro";
import {ProductsList} from "../data-list"
import ProductCard from "../components/ProductCard.astro";
---

<section id="products" class="relative py-20 bg-gradient-to-b from-black via-[#1A001D] to-black">
    <ContentWidth>
        <div class="text-center mb-12">
            <h2 class="text-4xl md:text-5xl font-serif text-white mb-4 drop-shadow-lg">
                Nuestros Productos
            </h2>
            <p class="text-gray-300 text-base md:text-lg max-w-2xl mx-auto leading-relaxed">
                Elige la presentación perfecta para cada ocasión: desde un detalle íntimo hasta una celebración con amigos.
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
            {ProductsList.map((product)=>(
                <ProductCard title={product.title} description={product.description} image={product.image} price={product.price} offer={product.offer} />
            ))}
        </div>
    </ContentWidth>
</section>
